// function calculateScore(submission) {
//     //if there is no submission, or the submission is missing, return 0
//     if (!submission || submission.missing) {
//         return 0;
//     }

//     const late = submission.late;
//     const grade = submission.grade;

//     if (submission.late) {
//         return 0.2;
//     }

//     let score = 0;
//     answers.forEach((answer) => {
//         if (answer.isCorrect) {
//             score += 1;
//         }
//     });
//     return score;
// }
